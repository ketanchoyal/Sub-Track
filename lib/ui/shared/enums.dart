part of "shared.dart";

enum TextFieldType {
  DEFAULT,
  ACTIVE,
  VALID,
  ERROR,
  DISABLED,
}

enum ButtonType {
  PRIMARY,
  SECONDARY,
  TERTIARY,
  DISABLED,
  ICON,
}

enum IconType {
  Services,
  Emoji,
}
